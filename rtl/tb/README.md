# RTL testbenches

Run all framebuffer/XBUS testbenches from the repository root with:

`powershell -ExecutionPolicy Bypass -File rtl/tb/run_xsim_tests.ps1`

The script requires Vivado XSIM tools (`xvhdl`, `xelab`, `xsim`) in `PATH`.
